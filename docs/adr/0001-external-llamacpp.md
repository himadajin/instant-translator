# アプリは推論プロセスを持たない

翻訳はローカルの `llama.cpp` で行うが、アプリはそのプロセスを起動も同梱も監視もしない。
利用者が `llama.cpp` サーバを自分で起動し、アプリは既定の接続先 `http://127.0.0.1:8080` へ
HTTP で接続するだけの利用者として振る舞う。
モデルの入手、量子化の選択、サーバの寿命と資源配分を利用者側へ置くことで、
アプリはバイナリ配布、プロセス管理、モデル更新の追随から切り離され、
接続状態の表示と失敗時の再試行だけを責務にできる。
